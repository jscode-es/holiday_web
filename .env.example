# Protects the whole site with HTTP Basic Auth. Leave both unset to disable
# (e.g. local dev). Set both as Environment Variables in the Vercel project
# dashboard for the public deployment — never commit real values here.
SITE_AUTH_USER=
SITE_AUTH_PASSWORD=
